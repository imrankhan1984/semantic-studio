/// <reference types="vite/client" />
// Vite's ambient types. Declares `import ... from "./x?raw"` and
// `import.meta.glob`, both used by the source-scanning tests to read files
// without pulling in @types/node for a handful of assertions.
