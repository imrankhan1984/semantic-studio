# Semantic Studio — Working on this codebase

**The instructions are in [`CLAUDE.md`](./CLAUDE.md). Read that file.**

This one exists only so that a tool looking for `AGENTS.md` finds its way there.
It is a pointer on purpose, not a copy.

A full copy did live here, for about two hours on 2026-07-27. It was stale
before anyone read it: it reported 48 backend tests when there were 139,
described security defects S-1 to S-4 as open after all four had been fixed and
merged, and pointed at a `.Codex/skills/` directory that exists nowhere in this
repository. An agent following it would have re-fixed finished work.

Two copies of one instruction file disagree faster than anyone notices. So there
is one copy, and this is the signpost to it.

Project skills are in `.claude/skills/`. There is no second copy of those
either, for the same reason.
